package com.swh.project;

public class Singleton {
	private static Singleton printer = null;

	private Singleton(){}

	public static Singleton getInstance() {
		if(printer == null) {
			printer = new Singleton();
		}
		return printer;
	}

	public void print(String input) {
		System.out.println(input);
	}
}
